POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
MCP_API_KEY=
# Basic Auth for /dashboard and /api/admin/* (Caddy). Generate the hash with:
#   docker run --rm caddy:2-alpine caddy hash-password --plaintext '<password>'
ADMIN_BASIC_AUTH_USER=
ADMIN_BASIC_AUTH_HASH=
